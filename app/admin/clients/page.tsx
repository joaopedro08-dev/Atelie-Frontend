import { ClientPageContent } from "@/components/admin/client/client-page-content";

export const metadata = {
    title: 'Clientes',
};

export default function ClientsPage() {
    return <ClientPageContent />;
}