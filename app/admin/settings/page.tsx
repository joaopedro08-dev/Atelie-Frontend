import { SettingsPageContent } from "@/components/admin/setting/settings-page-content";

export const metadata = {
    title: 'Configurações'
};

export default function SettingsPage() {
    return <SettingsPageContent />;
}