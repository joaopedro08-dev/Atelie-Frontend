import { OrderPageContent } from "@/components/admin/order/order-page-content";

export const metadata = {
  title: 'Pedidos',
};

export default function OrderPage() {
  return <OrderPageContent />;
}